/*
 * Velune - Parametric EQ filter types.
 * Ported from Echo Music (GPL-3.0).
 */

package com.nikhil.yt.eq.data

enum class FilterType {
    PEAK,
    LOW_SHELF,
    HIGH_SHELF,
    LOW_PASS,
    HIGH_PASS,
    BAND_PASS,
    NOTCH,
    ALL_PASS,
}
